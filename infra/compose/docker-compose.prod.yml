# ============================================================
# docker-compose.prod.yml — Linux 生产环境
# 无 bind mount / 无 dev env / 全镜像自包含
# 前端由 Nginx 容器 serve，perception 可直通摄像头
# ============================================================

services:
  # --- 基础设施 ---
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ${PG_USER:-javis}
      POSTGRES_PASSWORD: ${PG_PASSWORD}
      POSTGRES_DB: ${PG_DB:-javis}
    volumes:
      - pgdata:/var/lib/postgresql/data
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U javis"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "5"

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    expose:
      - "6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

  # --- 网关 ---
  api-gateway:
    image: studio-javis/api-gateway:${IMAGE_TAG:-latest}
    env_file: .env.production
    environment:
      SERVICE_NAME: api-gateway
      RUN_MIGRATIONS: "true"
      WAIT_PG_HOST: postgres
      WAIT_REDIS_HOST: redis
      APP_ENV: production
      AGENT_URL: http://agent:8000
      PERCEPTION_URL: http://perception:8000
      LOG_LEVEL: info
    expose:
      - "8000"
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "5"

  # --- Agent ---
  agent:
    image: studio-javis/agent:${IMAGE_TAG:-latest}
    env_file: .env.production
    environment:
      SERVICE_NAME: agent
      BRIDGE_PORT: "8000"
      PG_HOST: postgres
      WAIT_PG_HOST: postgres
      WAIT_REDIS_HOST: redis
      APP_ENV: production
      AGENT_URL: http://agent:8000
      PERCEPTION_URL: http://perception:8000
      LOG_LEVEL: info
    expose:
      - "8000"
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "5"

  # --- Perception ---
  perception:
    volumes:
      - /home/arona/.cache/modelscope:/home/javis/.cache/modelscope
    image: studio-javis/perception:${IMAGE_TAG:-latest}
    env_file: .env.production
    environment:
      SERVICE_NAME: perception
      WAIT_PG_HOST: postgres
      WAIT_REDIS_HOST: redis
      APP_ENV: production
      AGENT_URL: http://agent:8000
      PERCEPTION_URL: http://perception:8000
      LOG_LEVEL: info

    expose:
      - "8000"
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "5"


  # --- 前端 Nginx ---
  web:
    image: studio-javis/web:${IMAGE_TAG:-latest}
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - api-gateway
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

volumes:
  modelscope_cache:
  pgdata:
  redis_data:
