[package]
name = "desktop-protocol"
version = "0.1.0"
edition = "2021"
description = "跨语言共享类型 — Rust + TypeScript (前端 codegen)"
license = "MIT"

[lib]
name = "desktop_protocol"
path = "src/lib.rs"

[dependencies]
serde.workspace = true
serde_json.workspace = true
ts-rs.workspace = true
chrono.workspace = true
uuid.workspace = true

[dev-dependencies]
# specta 在 Phase 2 加, 这里占位

[lints]
workspace = true
