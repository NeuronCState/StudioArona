[package]
name = "tauri-bridge"
version = "0.1.0"
edition = "2021"
description = "Tauri commands 桥接 — 前端 ↔ desktop-core RPC"
license = "MIT"

[lib]
name = "tauri_bridge"
path = "src/lib.rs"

[dependencies]
# Phase 2 才用 tauri — Phase 1 stub 预留
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
desktop-core = { path = "../desktop-core" }
desktop-protocol = { path = "../protocol" }

[lints]
workspace = true
