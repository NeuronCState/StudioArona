{
  "name": "@studio-arona/agent",
  "version": "0.2.0",
  "private": true,
  "description": "Studio Arona — Agent service (Hermes + LLM Gateway compatible)",
  "type": "module",
  "scripts": {
    "start": "echo '请改用 make agent (LLM Gateway + Agent Bridge) 或 make hermes (Hermes TUI)。详见 docs/hermes-integration.md' && exit 1",
    "bridge": "node bridge/server.js",
    "llm-gateway": "uv run python -m uvicorn main:app --app-dir ../../services/llm_gateway --host 127.0.0.1 --port 8645"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "cheerio": "^1.2.0",
    "jsdom": "^25.0.1",
    "pg": "^8.21.0",
    "rss-parser": "^3.13.0",
    "ws": "^8.20.1"
  }
}
