# Plan: Create AGENTS.md for StudioArona

## Context

No `AGENTS.md` exists yet. The repo has a detailed `CLAUDE.md` (196 lines) that covers architecture, commands, and conventions. The `AGENTS.md` should complement it by focusing on agent-specific gotchas — things an AI coding agent would likely get wrong without guidance.

## Approach

Create `/Users/zhangxuanning/StudioArona/AGENTS.md` — a compact, high-signal instruction file tailored for coding agents (OpenCode, Claude Code, Cursor, etc.).

## Key facts to include (verified from source)

### Commands
- **Start**: `./run.sh start` (preferred) or `make dev`
- **Bootstrap**: `make bootstrap` (pnpm install + uv sync + docker PG/Redis + alembic + seed)
- **Single Python test**: `uv run pytest tests/D/unit/test_auth.py -k "test_login" -v`
- **Single frontend test**: `pnpm --filter web exec vitest run src/stores/auth.test.ts`
- **Lint**: `make lint` (ruff + eslint + spectral)
- **Typecheck**: `make typecheck` (mypy + tsc)
- **Type generation**: `make generate-types` after OpenAPI changes
- **Pre-commit**: lint-staged + ruff + gitleaks (via husky)
- **Commit format**: `type(scope): subject` — scopes: web/agent/perception/gateway/skills/contracts/infra/docs/deps

### Architecture gotchas
- Services A/B/C never talk directly — all through api-gateway or shared DB
- `packages/contracts/openapi.yaml` is the single source of truth — changes need 3-person review
- OpenClaw is deprecated — use LLM Gateway (`services/llm_gateway/`) on port 18789
- `services/llm_gateway/` is NOT in the uv workspace members — it's a standalone FastAPI app
- Test directories: `tests/A/` (frontend), `tests/B/` (agent), `tests/C/` (perception), `tests/D/` (gateway), `tests/E_infra/`
- Python managed with `uv`, Node with `pnpm` — never mix
- Mock flags: `MOCK_NAS`, `MOCK_HA`, `MOCK_VM`, `MOCK_HARDWARE`, `MOCK_SERIAL` in `.env.local`

### File conventions
- `.env.local` is the real config (gitignored) — copy from `.env.example`
- Ruff line-length=100, mypy strict with pydantic plugin
- pytest `asyncio_mode=auto`, pythonpath includes `services/api-gateway`
- pnpm workspaces: `apps/*`, `packages/*`, `services/*`

## Files to create/modify

- **Create**: `AGENTS.md` (new file, ~80-100 lines)

## Verification

- Read the created file to ensure accuracy
- Cross-check commands against `Makefile` and `pyproject.toml`
