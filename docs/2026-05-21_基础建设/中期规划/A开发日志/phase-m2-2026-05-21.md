# Phase M2 开发日志 · 工程师 A · 对话核心

> 日期：2026-05-21
> 分支：`a/phase-m2-chat-core`
> 状态：已完成
> 上一 Phase: [[phase-m1-2026-05-21]]

---

## Checklist 执行记录

### 1+2+3. ChatPage 重构 + 消息气泡 + 流式 token 动效 + tool_call 卡片 ✓

**ChatBubble.tsx** — 完全重写
- 用户消息：右对齐，`max-w-[600px]`，`rounded-2xl rounded-br-md`，`bg-[var(--color-accent-soft)]` 淡赭背景
- hover 显示 [复制 / 编辑] 工具栏
- AI 消息：左对齐，无背景纯文字流，32px Avatar + 工具栏 [复制 / 重新生成 / 👍 / 👎]
- Markdown 渲染：react-markdown + remark-gfm + rehype-highlight
- 复制功能带 Check 图标反馈

**ToolCallCard.tsx** — 新建
- 折叠态：单行 chip，图标 + skill 名 + summary + 展开箭头
- 展开态：framer-motion `AnimatePresence` 高度动画，显示 Input JSON / Result / Error
- 状态指示：运行中 spinner / 成功绿色勾 / 失败红色叉
- 失败用淡红色边框 `border-[var(--color-error)]`

**ChatPage.tsx** — 完全重写
- 消息流主栏 `max-w-[768px] mx-auto`，间距 `space-y-6`
- 空态：serif 标题 + 描述 + 4 个 suggestion chips（点击直接发送）
- render_card 集成：`setRenderCardHandler` 将卡片追加到消息流
- `javis:clear-session` 事件监听
- handleRegenerate：重建消息上下文重新发送

**流式 token 动效**
- 全局 CSS `.stream-cursor > *:last-child::after { content: '▌' }` 闪烁光标
- token 按 chunk 由 SSE 驱动渲染到 Markdown
- 不做逐 char motion，性能安全

### 5. 输入区附件 / 命令面板 ✓

**ChatInput.tsx** — 完全重写
- 自适应高度（最小 1 行，最大 200px）
- `⌘+Enter` 发送，`Enter` 换行
- 命令面板：输入 `/` 触发，5 个预设命令（RSS/日程/VM/状态/记忆），↑↓ 选择
- 左侧附件按钮，右侧麦克风 + 发送/停止
- 拖拽文件高亮虚线边框（dragOver 状态）
- 底部 hint 行：⌘+Enter 发送 · / 命令面板 · 支持拖放文件

### 4. Sidebar 历史会话分组与重命名 ✓

**SessionList.tsx** — 新建
- 按"今天 / 昨天 / 本周 / 更早"分组
- 每个会话项：标题（首条用户消息截断）+ 时间分组标签
- hover 出现 [重命名 / 删除] 图标按钮
- 行内编辑：输入框 + ✓/✕ 确认
- 从 `GET /api/chat/sessions` 获取会话列表

**session store** — 扩展
- 新增 `sessions: SessionSummary[]`
- `addSession / renameSession / removeSession` 操作
- 新增 `SessionSummary` 类型

**Mock handler** — 更新
- 新增 `GET /api/chat/sessions` 返回 5 条示例会话（涵盖所有时间分组）
- `POST /api/chat/sessions` 返回值增加 `title` 字段

### 6. Feeds / System / VMs 三页填充骨架 ✓

**SystemPage.tsx**
- 新增 `SystemSkeleton`：4 个圆形骨架 + 16 条柱状骨架 + 2 个矩形骨架
- `isLoading` 状态检测 + 错误态（图标 + 重试按钮）
- 颜色变量更新为 design tokens

**VmsPage.tsx**
- 新增 `VmListSkeleton`：4 个矩形骨架卡片
- 增强空态：Terminal 图标 + 标题 + 描述 + CTA 按钮"创建第一个 VM"
- VM 卡片增加图标和中文状态标签

**FeedsPage.tsx**
- 新增 `FeedsSkeleton`：6 个矩形骨架卡片
- 增强空态：Rss 图标 + 标题 + 描述 + CTA 按钮"添加第一个 RSS 源"
- 移除未使用的 Bell 按钮

---

## 改动文件汇总

### 新增 (3)

```
apps/web/src/pages/chat/ToolCallCard.tsx
apps/web/src/components/layout/SessionList.tsx
```

### 修改 (12)

```
apps/web/src/pages/chat/ChatPage.tsx
apps/web/src/pages/chat/ChatBubble.tsx
apps/web/src/pages/chat/ChatInput.tsx
apps/web/src/pages/chat/ChatPage.test.tsx
apps/web/src/components/layout/Sidebar.tsx
apps/web/src/stores/session.ts
apps/web/src/mocks/handlers.ts
apps/web/src/pages/system/SystemPage.tsx
apps/web/src/pages/vms/VmsPage.tsx
apps/web/src/pages/feeds/FeedsPage.tsx
apps/web/src/pages/feeds/FeedsPage.test.tsx
```

---

## 验证结果

| 检查项 | 结果 |
|--------|------|
| `pnpm --filter web typecheck` | ✓ 通过 |
| `pnpm --filter web test` | ✓ 66 passed, 13 files |
| `pnpm --filter web lint` | ✓ 通过（已修复依赖，commit `57729a5`） |

---

## Issues

### 已修复（2026-05-21）

1. ~~ESLint 无法运行~~ → 已安装 `typescript-eslint`、`eslint-plugin-react`、`eslint-plugin-react-hooks`，lint 零错误
2. ~~SessionList a-in-a~~ → 代码审查确认 SessionList 已使用 `<button>` + store action，无嵌套 `<a>` 问题
3. ~~ToolCallCard Storybook~~ → 新增 4 个 story：Running / Success / Error / Large JSON result
4. ~~§5 token 流动效规范~~ → 标注为 "M4 抛光时上 chunk 级 motion.span，M2 用 CSS stream-cursor"

### 后续 Phase 依赖

5. **TokenStream 逐 token 入场动效** → M4 抛光时上 chunk 级 motion.span（已标注在任务书 §5）
6. **ToolCallCard layoutId 动画** → M4 抛光阶段可接入 real framer-motion layout animations
7. **语音模式入口 `/voice`** → Phase M3
7. **Memory 页时间轴/网格/关系图** → Phase M3
8. **Schedule 页** → Phase M3
